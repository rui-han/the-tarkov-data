import { createTheme } from "@mui/material";

// dark theme with customization
export const darkTheme = createTheme({
  palette: {
    mode: "dark",
    background: {
      default: "#2D2D2F",
    },
    text: {
      primary: "#9a8866",
    },
  },
});
